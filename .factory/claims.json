[
  {
    "id": "local-demo",
    "claim": "Archive data stays in your browser.",
    "where": "landing facts and privacy page",
    "test": "npx playwright test --grep @claim:local-demo",
    "sandbox": "fresh browser context at /demo; edit a note; assert only the demo: key exists and every request stays same-origin"
  },
  {
    "id": "status-separation",
    "claim": "Dead pages and failed checks stay separate.",
    "where": "demo review groups and terms",
    "test": "npx playwright test --grep @claim:status-separation",
    "sandbox": "sample data; select each group and assert its distinct result and reason"
  },
  {
    "id": "html-import",
    "claim": "Imports standard browser bookmark HTML.",
    "where": "how it works and README",
    "test": "npx vitest run -t @claim:html-import",
    "sandbox": "bundled Netscape bookmark fixture; assert title, folder, timestamp, and record count"
  },
  {
    "id": "duplicate-detection",
    "claim": "Finds duplicate URLs after removing common tracking parameters.",
    "where": "README and product review group",
    "test": "npx vitest run -t @claim:duplicate-detection",
    "sandbox": "bundled bookmark fixture with a UTM variant and canonical URL"
  },
  {
    "id": "html-export",
    "claim": "Standard HTML export is always free.",
    "where": "landing facts, demo toolbar, and README",
    "test": "npx playwright test --grep @claim:html-export",
    "sandbox": "fresh /demo; download kept bookmarks; assert Netscape header and a kept sample record"
  },
  {
    "id": "paid-license",
    "claim": "A $18 one-time license removes the 50-check limit.",
    "where": "paid tier, extension license panel, and README",
    "test": "npx playwright test --grep @claim:paid-license",
    "sandbox": "fresh home page with a test license; mock the documented verify response; assert licensed state and namespaced storage"
  }
]
